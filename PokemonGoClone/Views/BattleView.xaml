<UserControl x:Class="PokemonGoClone.Views.BattleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokemonGoClone.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <UniformGrid Rows="2" Columns="2" Margin="32">
            <Image Source="{Binding CurrentViewModel.PlayerPokemon.ImageSource}"
                   Height="64"
                   Width="64"/>
            <Image Source="{Binding CurrentViewModel.OpponentPokemon.ImageSource}"
                   Height="64"
                   Width="64"
                   RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <ScaleTransform ScaleX="-1"/>
                </Image.RenderTransform>
            </Image>
        </UniformGrid>
    </Grid>
</UserControl>
