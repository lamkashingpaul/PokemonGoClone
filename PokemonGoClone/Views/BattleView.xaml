<UserControl x:Class="PokemonGoClone.Views.BattleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokemonGoClone.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <UniformGrid Rows="1" Columns="3" Margin="32">
            <Image Source="{Binding CurrentViewModel.PlayerPokemon.ImageSoure}"/>
            <Image Source="{Binding CurrentViewModel.OpponentPokemon.ImageSoure}"/>
        </UniformGrid>
    </Grid>
</UserControl>
