<UserControl x:Class="PokemonGoClone.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokemonGoClone.Views"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640">
    <Grid Focusable="False">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Focusable="True" Grid.Column="0" Grid.Row="0" Margin="10" FontSize="36" Content="New" Command="{Binding GoToTrainerCreationViewModelCommand}"/>
        <Button Focusable="True" Grid.Column="0" Grid.Row="1" Margin="10" FontSize="36" Content="Load" Command="{Binding GoToLoadViewModelCommand}"/>
        <Button Focusable="True" Grid.Column="0" Grid.Row="2" Margin="10" FontSize="36" Content="Exit" Command="{Binding CloseWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
    </Grid>
</UserControl>
