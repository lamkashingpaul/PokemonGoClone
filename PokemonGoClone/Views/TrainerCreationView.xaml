<UserControl x:Class="PokemonGoClone.Views.TrainerCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokemonGoClone.Views"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="128"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Column="0" Grid.Row="1" Margin="10" Columns="3">
            <StackPanel>
                <RadioButton x:Name="blue" HorizontalAlignment="Center" IsChecked="True"/>
            </StackPanel>
            <StackPanel>
                <RadioButton x:Name="red" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel>
                <RadioButton x:Name="green" HorizontalAlignment="Center"/>
            </StackPanel>
        </UniformGrid>

        <DockPanel Grid.Column="0" Grid.Row="2" Margin="10" LastChildFill="True">
            <Button DockPanel.Dock="Left" Content="Back to Start" Margin="10" FontSize="12" Command="{Binding GoToStartViewModelCommand}"/>
            <Button DockPanel.Dock="Right" Content="Confierm" Margin="10" FontSize="12" Command="{Binding TrainerCreationCommand}">
                <Button.CommandParameter>
                    <MultiBinding Converter="">
                        
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12">Enter Your Name: </TextBlock>
                <TextBox x:Name="nameOfTrainer" DockPanel.Dock="Right" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16"/>
            </DockPanel>
        </DockPanel>
    </Grid>
</UserControl>
